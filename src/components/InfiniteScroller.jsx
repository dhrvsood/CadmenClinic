// This is a skeleton starter React component generated by Plasmic.
// This file is owned by you, feel free to edit as you see fit.
import * as React from "react";
import { PlasmicInfiniteScroller } from "./plasmic/cadmenclinic_migration/PlasmicInfiniteScroller";
import { useRef, useEffect } from "react";
import gsap from 'gsap'

function InfiniteScroller_(props, ref) {
  const scrollerRef = useRef(null);

  useEffect(() => {
    if (scrollerRef.current) {
      gsap.to(scrollerRef.current, {
        duration: 45,
        x: '-50%',
        ease: 'none',
        repeat: -1,
      });
    }
  }, [])

  return <PlasmicInfiniteScroller root={{ ref }} {...props} infiniteAnimator={{ref: scrollerRef}} />;
}

const InfiniteScroller = React.forwardRef(InfiniteScroller_);

export default InfiniteScroller;
